---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: yandexidentities.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: YandexIdentity
    listKind: YandexIdentityList
    plural: yandexidentities
    singular: yandexidentity
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: YandexIdentity is the Schema for the YandexIdentitys API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: YandexIdentitySpec defines the desired state of YandexIdentity.
            properties:
              keyname:
                description: KeyName is the name of the key in the secret. Default
                  is `YandexCloudSAKey`.
                type: string
              secretname:
                description: |-
                  Name is the name of a secret in the same namespace as YandexIdentity.
                  The secret must contain a key, which contains an valid iamkey.Key as of github.com/yandex-cloud/go-sdk/iamkey.
                type: string
            required:
            - secretname
            type: object
          status:
            description: YandexIdentityStatus defines the observed state of YandexIdentity.
            properties:
              ready:
                description: Ready is true when the secret is checked and resource
                  is ready.
                type: boolean
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
